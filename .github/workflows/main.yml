name: CI

on:
  push:
    branches: [ main ]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v2
        
      - name: Setup Node.js environment
        uses: actions/setup-node@v2.1.2
        with: 
          node-version: 12.x
          
      # Instalar dependencias NPM/Yarn
      - name: Install dependences
        run: yarn
        
      # Executar Build
      - name: Run Build
        run: yarn build

      # Copiar pasta /dist para Digital Ocean
      - name: Copy /dist to Digital Ocean
        uses: appleboy/scp-action@master
        with:
          host: ${{ secrets.SSH_HOST }}
          username: ${{ secrets.SSH_USER }}
          port: ${{ secrets.SSH_PORT }}
          key: ${{ secrets.SSH_KEY }} 
          source: "."
          target: "~/app/gobarber-backend"
        
        
      



# Substituir o código existente de /dist pelo novo código
# Executar YARN na Digital Ocean
# Executar as migrations typeorm
# Estart servidor Node.js

        
